# Universal-boolean-algebra-table
A Python Code with input that prints the boolean algebra table for every funktion with the operators("and", "not", "(", ")")
#
The Variabels can have every name except the operators name
